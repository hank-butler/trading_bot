const { expect } = require('chai')

const config = require('../config.json')

describe('Arbitrage', () => {
    let owner, arbitrage

    // Get signers
    // Deploy contract
    // Need Sushi and UnisWap (or other Uni Swap fork)
})